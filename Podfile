# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'GullyTrends' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for GullyTrends
     pod 'Parse'
       pod 'FacebookCore'
       pod 'FacebookLogin'
       pod 'MBProgressHUD', '~> 1.1.0'
end
